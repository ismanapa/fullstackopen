const initialNotification = '';

const notificationReducer = (state = initialNotification, aciton) => {
    switch (state.type) {
        default:
            return state;
    }
};

export default notificationReducer;